pub const BASE_URL_INDEX: &str = "https://index.crates.io";
pub const BASE_URL_CRATES: &str = "https://crates.io/api/v1/crates";

pub const QUERY_STRING_CRATES: &str = "?include=downloads"; // Fetches only the most basic data instead of full

pub const CRAWL_MAX_INTERVAL_SECONDS: u64 = 5;
